# 🏗️ Ghost Micro: Modular Architecture (สถาปัตยกรรมแบบแยกส่วน)

เพื่อให้โครงการสามารถขยายตัวได้อย่างยั่งยืนและเปิดรับการอัปเกรดแบบ Plug-and-Play เราจึงแบ่งส่วนการพัฒนาออกเป็น 5 โมดูลหลักที่มีหน้าที่และตรรกะการทำงาน (Logic) ชัดเจนดังนี้:

---

## 1. 🎮 ส่วนควบคุมทางไกล (Transmitter Module)
**หน้าที่:** เป็นทางเลือกสำรอง (Emergency Manual Control) สำหรับควบคุมหุ่นยนต์โดยไม่ต้องใช้คอมพิวเตอร์
- **Hardware:** ESP32 + Joystick Units + Switch + OLED
- **ประโยชน์:** ในกรณีฉุกเฉินหรือพื้นที่ที่ไม่มีสัญญาณ WiFi/PC สามารถใช้งานรีโมทควบคุมได้ทันที
- **Communication:** ส่งข้อมูลไปยัง Header ผ่าน ESP-NOW หรือ MAVLink (กรณีใช้ร่วมกับ Pixhawk)

---

## 2. 🧠 ส่วนสมองนำร่อง (Header / Receiver Module)
**หน้าที่:** เป็น "หัว" (Brain Center) ที่ประมวลผลคำสั่งระดับสูงและรวบรวม Telemetry
- **Hierarchy:** 
    - **ESP32 Communication Bridge:** รับข้อมูลจาก Transmitter หรือ Dashboard
    - **STM32F1 Logic Center:** ประมวลผลตรรกะหลัก วิเคราะห์คำสั่ง และส่งต่อให้ส่วนร่างกาย
- **ขีดความสามารถ:** 
    - รับคำสั่งจาก ESP32 -> ส่งต่อ Nano (Body)
    - รวบรวมข้อมูลเซนเซอร์จาก Nano -> รายงานกลับ ESP32 -> ส่งกลับ Transmitter/Dashboard
- **Upgrade Path:** มีพอร์ตสำหรับเชื่อมต่อโมดูลเสริมเพื่อ "อัปเกรดสมอง" ในอนาคต (เช่น AI Vision, Voice Recognition)

---

## 🦾 3. [ส่วนดวงตาและภาพถ่าย (Vision Module)](module_vision.md)
**หน้าที่:** สตรีมวิดีโอและทำ Image Recognition (AI) เพื่อค้นหาผู้ประสบภัย

---

## 📦 4. ส่วนร่างกายและกลไก (Body Module)
**หน้าที่:** ควบคุมระบบขับเคลื่อน ระบบไฟแรงสูง และการรับรู้รอบตัวพื้นฐาน
- **Hardware:** Arduino Nano (เป็นหลัก) + Motor Driver + Sensor Array
- **Independent Control Logic (สำคัญ):**
    - **Mode A (Standalone):** สามารถควบคุมผ่าน Bluetooth ได้โดยตรง (สำหรับงานพื้นฐาน)
    - **Mode B (Header Override):** เมื่อมีการเสียบ "หัว" (Header) เข้ากับร่างกาย Nano จะตัดการเชื่อมต่อ Bluetooth ทันที และฟังคำสั่งจาก Header เท่านั้น
- **หน้าที่เสริม:** จัดการระบบไฟที่เกิน 5V (Propulsion Power) และควบคุม Ultrasonic/IMU รอบตัว

---

## 🛠️ 5. ส่วนโมดูลอัปเกรดร่างกาย (Body Upgrades)
**หน้าที่:** เพิ่ม "ขีดความสามารถทางกายภาพ" ให้กับหุ่นยนต์
- **ลักษณะ:** เชื่อมต่อกับบอร์ด Body (Arduino) หรือมี Logic ของตัวเองที่คุยกับ Header
- **ตัวอย่าง:**
    - **Robotic Arm:** ระบบแขนกลสำหรับหยิบจับวัตถุในจุดกู้ภัย
    - **Extra Battery Hub:** ชุดขยายพลังงานสำหรับการปฏิบัติงานระยะไกล
    - **Special Treads:** ชุดล้อตีนตะขาบสำหรับมุดซากตึก

---

## 🔄 สรุปกระแสข้อมูล (Data Flow Summary)
`Transmitter/Dashboard` <-> `ESP32 (Bridge)` <-> `STM32 (Logic)` <-> `Arduino Nano (Body Hub)` <-> `Actuators/Sensors`

> [!IMPORTANT]
> สถาปัตยกรรมนี้ช่วยให้นายสามารถพัฒนาแต่ละส่วนแยกกันได้ 100% ครับ เช่น นายอาจจะทำแขนกล (Body Upgrade) รอไว้ก่อน โดยที่ไม่ต้องยุ่งกับโค้ดนำทางใน STM32 เลยครับ!
