# Ghost Micro V5: Wiring & Hardware Guide

การประกอบ **Ghost Micro V5** จะแตกต่างจากเวอร์ชันก่อนหน้าอย่างสิ้นเชิง เพราะเราใช้ **Pixhawk** เป็นศูนย์กลางการเชื่อมต่อแทน STM32

---

## ⚡ 1. ระบบจ่ายไฟ (Power System)
**สำคัญมาก:** Pixhawk ห้ามจ่ายไฟ 5V ผ่านขา I/O ทั่วไปโดยไม่มีการป้องกัน
- **Pixhawk Power Module:**
  - ต่อ XT60 เข้ากับแบตเตอรี่ (2S - 6S)
  - สายแพ 6-pin ต่อเข้าช่อง **POWER** ของ Pixhawk
  - *Module นี้จะจ่ายไฟ 5.3V ที่นิ่งมากและวัดกระแส/แรงดันส่งให้ Pixhawk ด้วย*

---

## 📡 2. การเชื่อมต่อสัญญาณ (Signal Connections)

| Pixhawk Port | Component | Connection Type | Note |
| :--- | :--- | :--- | :--- |
| **GPS** | M8N GPS Module | 6-pin JST-GH | ข้อมูลตำแหน่งดาวเทียม |
| **I2C** | **Crius I2C Hub** | 4-pin JST-GH | **Central Hub:** ต่อ Compass, OLED, Nano เข้าด้วยกัน |
| **RCIN** | FlySky FS-iA6B | 3-pin (iBus/PPM) | รับสัญญาณวิทยุ 2.4GHz |
| **TELEM1** | **ESP32 Gateway** | Serial (57600) | ส่งข้อมูลไป Dashboard |
| **TELEM2** | STM32 (Optional) | Serial (57600) | ถ้ายังต้องการใช้ STM32 ประมวลผลเพิ่ม |
| **MAIN OUT 1** | L298N (ล้อซ้าย) | PWM | คุมความเร็ว/ทิศทาง |
| **MAIN OUT 3** | L298N (ล้อขวา) | PWM | คุมความเร็ว/ทิศทาง |

---

## 🚦 3. การต่อ ESP32 Gateway (MAVLink Bridge)
เราจะใช้ ESP32 เป็นสะพานเชื่อม WiFi ของเราเข้ากับ Pixhawk

```
ESP32 (V5)          Pixhawk (TELEM1)
VIN (5V)      <-->  PIN 1 (VCC 5V)
GND           <-->  PIN 6 (GND)
GPIO 34 (RX)  <-->  PIN 2 (TX)
GPIO 27 (TX)  <-->  PIN 3 (RX)
```
*หมายเหตุ: Pixhawk TELEM1 ทำงานที่ 57600 Baud เป็นค่าเริ่มต้น (ArduPilot Default)*

---

## 🔗 4. การขยายพอร์ต I2C (Crius I2C Hub)
เนื่องจาก Pixhawk มีพอร์ต I2C จำกัด เราจะใช้ **Crius I2C Hub** เพื่อแชร์พอร์ตเดียวให้กับหลายอุปกรณ์:
1.  **Input:** ต่อสาย 4-pin จากพอร์ต **I2C** ของ Pixhawk เข้าช่องใดก็ได้ของ Hub
2.  **Output:** 
    - ต่อสายจาก **Compass** (ติดกับ GPS) เข้าช่องที่เหลือบน Hub
    - ต่อสายไปยัง **Arduino Nano** (A4/A5) เพื่อส่งข้อมูล Ultrasonic
    - ต่อสายไปยัง **OLED Display** เพื่อแสดงสถานะ
ArduRover จะส่งสัญญาณ PWM ออกมาที่ช่อง **MAIN OUT**:
- **CH1:** Steering (เลี้ยว) หรือ Left Motor (กรณีดริฟท์)
- **CH3:** Throttle (เดินหน้า) หรือ Right Motor (กรณีดริฟท์)
*ติดตั้งผ่านเมนู Frame Setup ใน Mission Planner*

---

## ✅ Checklist ก่อนเปิดไฟครั้งแรก
- [ ] เช็คว่าไม่มีสายไฟเปลือยแตะกัน (โดยเฉพาะช่อง POWER)
- [ ] ติดตั้ง GPS ในตำแหน่งที่ไม่มีอะไรบัง (โล่งๆ เหนือหุ่นยนต์)
- [ ] ต่อ Buzzer และ Safety Switch ที่มาในชุด Pixhawk (จำเป็นต้องใช้เพื่อคุมสถานะ Arming)

---

> [!TIP]
> **Mission Planner:** แนะนำให้ดาวน์โหลด [Mission Planner](https://ardupilot.org/planner/docs/common-install-mission-planner.html) ลงในคอมพิวเตอร์เพื่อใช้ในการ Calibrate บอร์ด Pixhawk และ GPS ให้แม่นยำครับ
