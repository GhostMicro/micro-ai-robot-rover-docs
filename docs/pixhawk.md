# 🦅 รายละเอียดบอร์ด Pixhawk 2.4.8

แพลตฟอร์มนี้รองรับการนำระบบควบคุมการบินมาใช้ในระยะยาว สำหรับการอัปเกรดหุ่นยนต์ Ghost Micro เป็นระดับมืออาชีพ

---

## 1. ข้อมูลทั่วไป: Pixhawk 2.4.8 (Pixhawk 1)
**Pixhawk 2.4.8** เป็นระบบควบคุมการบินอัตโนมัติขั้นสูงที่ออกแบบและผลิตโดย PX4 Open Hardware Project และ 3D Robotics โดดเด่นด้วยเทคโนโลยีโปรเซสเซอร์และเซ็นเซอร์ขั้นสูงจาก ST รวมถึงระบบปฏิบัติการแบบเรียลไทม์ **NuttX** ซึ่งให้ประสิทธิภาพ ความยืดหยุ่น และความน่าเชื่อถือสูง

### ✨ คุณสมบัติเด่น:
*   **Processor:** ARM Cortex-M4 32 บิต ประสิทธิภาพสูง ทำงานบน NuttX RTOS
*   **I/O และการเชื่อมต่อ:**
    *   เอาต์พุต PWM/เซอร์โว 14 ช่อง (8 ช่องหลัก + 6 ช่องเสริม)
    *   รองรับอุปกรณ์ต่อพ่วงครบครัน: UART, I2C, SPI, CAN
*   **Redundancy (ระบบสำรอง):** มีระบบจ่ายไฟสำรองและตัวควบคุมความปลอดภัยพื้นฐานในตัว สามารถสลับไปใช้ระบบสำรองได้ทันทีเมื่อระบบหลักขัดข้อง
*   **Power Management:** ระบบสลับแหล่งจ่ายไฟอัตโนมัติ พร้อมปุ่ม Safety Switch ภายนอก
*   **Feedback:** ไฟ LED แสดงสถานะหลากสี (High Power) และลำโพง Buzzer หลายโทน
*   **Logging:** ช่องเสียบ Micro SD สำหรับบันทึกข้อมูลการบิน (Data Logging) ความเร็วสูง

> [!NOTE]
> ภาพประกอบคุณสมบัติ: `ภาพ-20231017171021915`

---

## 2. การตั้งค่า RflySim
หากใช้ฮาร์ดแวร์ **Pixhawk 2.4.8 (Flash 2MB)** (ใช้เฟิร์มแวร์เป้าหมาย `px4_fmu-v3_default`) แนะนำให้ใช้การกำหนดค่าดังนี้:

*   **Compile Command:** `px4_fmu-v3_default`
*   **Firmware Version:** PX4 v1.13.2
*   **Compiler:** WinWSL

> [!TIP]
> **Pixhawk 1** มีไฟ LED ในตัว ไม่จำเป็นต้องต่อโมดูลภายนอก แต่สำหรับ Pixhawk 2/3/4 มักจะต้องใช้โมดูล LED แยก

---

## 3. การติดตั้งเฟิร์มแวร์อย่างเป็นทางการ (Recovery)
การกู้คืนหรือติดตั้งเฟิร์มแวร์ผ่านสถานีภาคพื้นดิน **QGroundControl (QGC)**:

1.  **การเชื่อมต่อ:** เปิด QGC และเชื่อมต่อตัวควบคุมการบินเข้ากับคอมพิวเตอร์
2.  **เลือกเฟิร์มแวร์:** ในหน้าต่าง Popup ให้เลือกไฟล์เฟิร์มแวร์ `px4_fmu-v3_default.px4`
3.  **รอการติดตั้ง:** รอจนกว่ากระบวนการแฟลชจะเสร็จสิ้น 100%
4.  **Configuration:**
    *   ตั้งค่าแร็ค (Airframe) เป็น **HIL Quadcopter X**
    *   กด "Apply and Restart" และรอการรีบูต
5.  **Radio Setup:** เชื่อมต่อตัวรับสัญญาณรีโมทคอลโทรลและทำการปรับเทียบ (Calibration) ตามแผนภาพ

---

## 4. การจำลองฮาร์ดแวร์ (Hardware-in-the-loop: HIL)
เมื่อตั้งค่าทุกอย่างเสร็จสิ้น สามารถเริ่มการจำลองได้ดังนี้:

1.  ปิด QGC
2.  รันสคริปต์ `HITLRun.lnk` ในโหมด Administrator
3.  ระบุหมายเลขพอร์ต COM ของตัวควบคุมการบินในหน้าต่าง CMD
4.  รอจนกว่า CopterSim จะแสดงข้อความ:
    `PX4: GPS 3D fixed & EKF initialization finished.`
5.  **ปลดล็อก (Arming):** ใช้ช่องสัญญาณ CH5 ในรีโมทคอนโทรลเพื่อเริ่มการบินและควบคุม

---
**สถานะการบันทึก:** ข้อมูลอัปเดตล่าสุดสำหรับการเตรียมระบบจำลองและฮาร์ดแวร์จริง